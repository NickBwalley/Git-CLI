PUSHING A NEW FILE TO AN EXISTING GIT REPOSITORY
->git init 
->git remote add origin <url>
->git pull origin master
->git add <file>
->git commit -m <message>
->git push origin master

EXPLANATION
(git init ) => initializes the file to a git repository.
(git remote add origin <url>) => to merge the new file to prepare it to the existing git repository
(git pull origin master) => merge the file 
(git add <file>) => add the file to be part of the existing repository
(git push origin master) => merges and push the file together 
