if you want to erase your whole history and start over again;
	cd <repo>
	rm -rf .git
	git init
If you want to remove both files and history;
	cd <repo>
	rm -rf *
	git init 
if you are linked with a remote repository: if you want to start over again,
but don't really mind some old history remaining; there is a quick way: